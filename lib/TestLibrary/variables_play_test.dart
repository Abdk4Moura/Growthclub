// List Growable
// List contacts = List.empty(growable: true);
// // List fixed-length
// List<int> contact = List.filled(10, 0);
// contact[0] = 10;
// // Lists - In Dart List is an array
// List listOfFilters = ['company', 'city', 'state'];

// listOfFilters.forEach((filter) {
// print('filter: $filter');
// });