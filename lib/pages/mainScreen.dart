import 'package:flutter/material.dart';

class MainScreenPageWidget extends StatelessWidget {
  const MainScreenPageWidget({super.key});

  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    throw UnimplementedError();
  }
}
